spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes: []
  config:
    enabled: false

eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
